<?xml version="1.0"?>

<compsets version="2.0">

  <help>
    =========================================
    compset naming convention
    =========================================
    The compset longname below has the specified order
    atm, lnd, ice, ocn, river, glc wave esp cesm-options

    The notation for the compset longname below is
    TIME_ATM[%phys]_LND[%phys]_ICE[%phys]_OCN[%phys]_ROF[%phys]_GLC[%phys]_WAV[%phys][_ESP][_BGC%phys]

    The following compsets are those that can be tested in CIME stand-alone configurations
    without any prognostic components.
    For the compsets below the following are the only allowable values of the components.

    TIME = Time period (e.g. 2000, HIST, RCP8...)
    ATM  = [DATM, SATM, XATM]
    LND  = [DLND, SLND, XLND]
    ICE  = [DICE, SICE, XICE]
    OCN  = [DOCN, SOCN, XOCN]
    ROF  = [DROF, SROF, XROF]
    GLC  = [      SGLC      ]
    WAV  = [DWAV, SWAV      ]
    ESP  = [DESP, SESP      ]

    The OPTIONAL %phys attributes specify submodes of the given system
    For example DOCN%DOM is the data ocean model for DOCN
    ALL data models must have a %phys option that corresponds to the data  model mode

    Each compset node is associated with the following elements
    - lname
    - alias
    - support  (optional description of the support level for this compset)
    Each compset node can also have the following attributes
    - grid  (optional regular expression match for grid to work with the compset)
  </help>

  <compset>
    <alias>AA</alias>
    <lname>tim:2000_wav:DWAV%CLIMO_rof:DROF%NYF_atm:DATM%NYF_lnd:DLND%LCPL_ice:DICE%SSMI_ocn:DOCN%DOM</lname>
  </compset>

  <compset>
    <alias>A</alias>
    <lname>scn:2000_atm:DATM%NYF_ice:DICE%SSMI_ocn:DOCN%DOM_rof:DROF%NYF</lname>
  </compset>

  <compset>
    <alias>AAIO</alias>
    <lname>scn:2000_atm:DATM%NYF_ice:DICE%SSMI_ocn:DOCN%DOM_rof:DROF%NYF_aio:APIO</lname>
  </compset>

  <compset>
    <alias>ADSOM</alias>
    <lname>scn:2000_atm:DATM%NYF_ice:DICE%SSMI_ocn:DOCN%SOM_rof:DROF%NYF_TEST</lname>
  </compset>

  <compset>
    <alias>ADSOMAQP</alias>
    <lname>scn:2000_atm:DATM%NYF_ocn:DOCN%SOMAQP</lname>
  </compset>

  <compset>
    <alias>ADAQP3</alias>
    <lname>scn:2000_atm:DATM%NYF_ocn:DOCN%AQP3</lname>
  </compset>

  <compset>
    <alias>ADAQPFILE</alias>
    <lname>scn:2000_atm:DATM%NYF_ocn:DOCN%AQPFILE</lname>
  </compset>

  <compset>
    <alias>A1850DLND</alias>
    <lname>scn:1850_lnd:DLND%SCPL</lname>
  </compset>

  <compset>
    <alias>ADWAV</alias>
    <lname>scn:2000_wav:DWAV%CLIMO</lname>
  </compset>

  <compset>
    <alias>ADESP</alias>
    <lname>scn:2000_atm:DATM%NYF_ocn:DOCN%SOMAQP_esp:DESP%NOOP</lname>
  </compset>

  <compset>
    <alias>ADESP_TEST</alias>
    <lname>scn:2000_atm:DATM%NYF_ocn:DOCN%SOMAQP_esp:DESP%TEST</lname>
  </compset>

  <compset>
    <alias>AIAF</alias>
    <lname>scn:2000_atm:DATM%IAF_ice:DICE%IAF_ocn:DOCN%IAF_rof:DROF%IAF</lname>
  </compset>

  <compset>
    <alias>S</alias>
    <lname>scn:2000</lname>
  </compset>

  <compset>
    <alias>X</alias>
    <lname>scn:2000_atm:XATM_lnd:XLND_ice:XICE_ocn:XOCN_rof:XROF_glc:XGLC_wav:XWAV</lname>
  </compset>

</compsets>
